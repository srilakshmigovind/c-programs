#include<stdio.h>
 int main()
{
 int n,r=0,s=0,temp;
 printf("enter the n value\n");
  scanf("%d",&n);
  temp=n;
 while(n!=0)
 {
      r=n%10;
      s=s+(r*r*r);
      n=n/10;
 }
 if(temp==s)
 printf("armstrong");
 else
 printf("not armstrong");
}
