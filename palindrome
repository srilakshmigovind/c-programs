#include<stdio.h>
main()
{
	int temp,n,rev=0;
	printf("enter the n value\n");
	scanf("%d",&n);
	temp=n;
	while(temp>0)
	{
		rev=rev*10;
		rev=rev+temp%10;
		temp=temp/10;
	}
if(n==rev)
printf("palindrome",n);
else
printf("not palindrome",n);
}
